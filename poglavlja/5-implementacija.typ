#import "../funkcije.typ": todo

= Имплементација правила
<implementacija>

У овом поглављу описана је имплементација свих 21 правила алата.
Свако правило имплементира трејт `Rule` и добија приступ КСС стаблу и
оригиналном изворном тексту. Правила су подељена у два скупа: општа
правила добре праксе, описана у овом поглављу, и правила инспирисана
књигом _100 Go Mistakes_ @100mistakes, описана у следећем поглављу.

== Општа правила добре праксе

=== Празан блок кода (`empty_block`)

Празан блок кода је блок са телом без наредби. Иако је синтаксно
исправан, углавном представља грешку или заборављен код.

#figure(
```go
if err != nil {
    // заборављена обрада грешке
}
```,
  caption: [Пример празног блока кода],
) <lst:empty-block-go>

Правило обилази стабло рекурзивним обиласком у дубину и за сваки
чвор типа `block` броји именоване потомке употребом методе
`named_child_count`. Ако је тај број нула, блок је празан и
генерише се дијагностика.

#figure(
```rust
if node.kind() == "block" {
    let named = node.named_child_count();
    if named == 0 {
        diagnostics.push(Diagnostic::new(
            file, "Empty code block detected",
            EmptyBlock.name(), node.start_position(),
        ));
    }
}
```,
  caption: [Провера броја именованих потомака чвора `block`],
) <lst:empty-block-rs>

=== Недостижни код (`unreachable_code`)

Наредбе које следе после безусловне терминирајуће наредбе (`return`,
`break`, `continue`, `fallthrough` или позив `panic`) никада неће бити
извршене.

#figure(
```go
func f() {
    return
    fmt.Println("никада се не извршава")
}
```,
  caption: [Пример недостижног кода],
) <lst:unreachable-go>

Правило прегледа сваки чвор `statement_list` и линеарно пролази кроз
именоване потомке одржавајући заставицу `found_terminator`. Чим се
наиђе на терминирајућу наредбу, заставица се поставља и свака
наредна наредба у истом листу добија дијагностику. Позив `panic` се
препознаје тако што се провери да ли је чвор `expression_statement`
чији именовани потомак је `call_expression` са именом функције `panic`.

#figure(
```rust
let mut found_terminator = false;
for i in 0..node.named_child_count() {
    if let Some(stmt) = node.named_child(i as u32) {
        if found_terminator {
            diagnostics.push(/* дијагностика */);
            continue;
        }
        if Self::is_terminating(stmt, source) {
            found_terminator = true;
        }
    }
}
```,
  caption: [Линеарни пролаз кроз листу наредби са заставицом],
) <lst:unreachable-rs>

=== Игнорисана грешка (`ignored_error`)

У Go-у, функције које могу да генеришу грешку по конвенцији враћају
вредност типа `error` као последњи повратни аргумент. Додељивање те
вредности бланк идентификатору `_` тихо је занемарује.

#figure(
```go
val, _ := strconv.Atoi("abc") // грешка конверзије се игнорише
```,
  caption: [Тихо игнорисање грешке бланк идентификатором],
) <lst:ignored-error-go>

Правило обилази чворове `short_var_declaration` и
`assignment_statement` и прегледа све идентификаторе на левој страни
доделе (поље `left`). Ако неки идентификатор има текстуалну вредност
`_`, генерише се дијагностика. Правило пријављује сваки бланк
идентификатор, без обзира на тип вредности коју прима, будући да
информације о типовима нису доступне у приступу заснованом на КСС
стаблу.

#figure(
```rust
"short_var_declaration" | "assignment_statement" => {
    if let Some(lhs) = node.child_by_field_name("left") {
        for i in 0..lhs.named_child_count() {
            if let Some(id) = lhs.named_child(i as u32) {
                if id.utf8_text(source.as_bytes())
                        .unwrap_or("") == "_" {
                    diagnostics.push(/* дијагностика */);
                }
            }
        }
    }
}
```,
  caption: [Провера левих операнада доделе на бланк идентификатор],
) <lst:ignored-error-rs>

=== Некоришћена променљива (`unused_variable`)

Декларисана, а никада коришћена променљива указује на заборављен код
или логичку грешку. Иако Go компајлер већ одбија непакетске
некоришћене променљиве, правило покрива и случајеве у оквиру истог
`statement_list` чвора где компајлер не пријављује грешку.

#figure(
```go
func f() {
    x := 42   // x никада се не употребљава
    y := 10
    fmt.Println(y)
}
```,
  caption: [Пример некоришћене променљиве],
) <lst:unused-go>

Имплементација је двофазна. У првој фази, пролази се кроз именоване
потомке чвора `statement_list` и у хеш-мапу `declared` прикупљају
се сви идентификатори декларисани путем `short_var_declaration` и
`var_declaration`, уз искључивање бланк идентификатора `_`.
У другој фази, рекурзивно се прикупљају сви идентификатори унутар
блока у листу и бројеви јављања складиштени су у хеш-мапи `used`.
Ако се идентификатор јавља само један пут, то значи да се јавља
искључиво на месту декларације и никада се не употребљава.

#figure(
```rust
// Фаза 2: бројање јављања
let mut identifiers = Vec::new();
Self::collect_identifiers(node, source, &mut identifiers);
for name in identifiers {
    *used.entry(name).or_insert(0) += 1;
}
// Детекција: јавља се само у декларацији
for (name, decl_node) in declared {
    if used.get(&name).cloned().unwrap_or(0) <= 1 {
        diagnostics.push(/* дијагностика */);
    }
}
```,
  caption: [Двофазна детекција некоришћених променљивих],
) <lst:unused-rs>

=== Правила увоза (`import_rules`)

Правило детектује два проблема: дупликате увоза (исти пакет увезен
двапут) и некоришћене увозе (пакет увезен, али нигде не употребљен).

#figure(
```go
import (
    "fmt"
    "fmt"  // дупликат
    "os"   // некоришћен
)
```,
  caption: [Дупликат и некоришћен увоз],
) <lst:imports-go>

У првом пролазу, рекурзивно се прикупљају сви чворови `import_spec`.
Из сваког се извлачи путања пакета (чвор `interpreted_string_literal`)
и локално ime (чвор `identifier` ако постоји псеудоним, иначе
последњи сегмент путање). У другом пролазу, прикупљају се сви
чворови `selector_expression` и из поља `operand` читају имена
употребљених пакета. На крају се проверава да ли је путања
увоза виђена раније (дупликат) и да ли је локално ime пакета
присутно у скупу употребљених пакета (некоришћен увоз).

#figure(
```rust
for (path, local_name, position) in imports {
    if seen.contains_key(&path) {
        diagnostics.push(/* дупликат */);
    } else {
        seen.insert(path.clone(), true);
    }
    if local_name != "_" && !used_packages.contains(&local_name) {
        diagnostics.push(/* некоришћен */);
    }
}
```,
  caption: [Провера дупликата и некоришћених увоза],
) <lst:imports-rs>

=== Засенчење променљиве (`variable_shadowing`)

Засенчење (енг. _variable shadowing_) настаје када унутрашњи опсег
декларише нову променљиву истог имена као постојећа из спољашњег
опсега. Оригинална вредност постаје недоступна унутар унутрашњег
блока, што може проузроковати суптилне логичке грешке.

#figure(
```go
val := 10
if val > 5 {
    val := 20         // засенчује спољашњи val
    fmt.Println(val)  // исписује 20
}
fmt.Println(val)      // исписује 10, не 20
```,
  caption: [Пример засенчења променљиве],
) <lst:shadowing-go>

Правило одржава стек опсега: листу хеш-мапа, где свака хеш-мапа
представља скуп декларисаних променљивих у одређеном опсегу.
При уласку у нови `block` или `statement_list` чвор, прикупљају
се све `short_var_declaration` и `var_declaration` декларације.
За сваку нову декларацију, претражују се сви родитељски опсези;
ако нека родитељска хеш-мапа садржи исто ime, пријављује се
засенчење. По завршетку обраде блока, тренутни опсег се скида
са стека пре него што се настави обилазак суседних чворова.

#figure(
```rust
for parent_scope in parent_scopes.iter().rev() {
    if parent_scope.contains_key(name) {
        diagnostics.push(Diagnostic::new(
            file,
            format!("Variable `{}` shadows variable \
                     from outer scope", name),
            VariableShadowing.name(),
            id.start_position(),
        ));
        break;
    }
}
current_scope.insert(name.to_string(), id);
```,
  caption: [Провера засенчења претраживањем стека опсега],
) <lst:shadowing-rs>

=== Одлагање ресурса унутар петље (`defer_in_loop`)

Наредба `defer` у Go-у одлаже извршавање функције до тренутка повратка
из текуће функције, а не до краја тренутне итерације петље. Употреба
`defer` унутар петље стога акумулира одложена позивања кроз све
итерације и ослобађа ресурсе тек по изласку из функције, уместо
одмах по завршетку сваке итерације.

#figure(
```go
for _, f := range files {
    defer f.Close() // f.Close се позива тек кад функција врати вредност
}
```,
  caption: [Пример погрешне употребе `defer` унутар петље],
) <lst:defer-go>

Правило претражује стабло рекурзивним обиласком и за сваки чвор
`for_statement` покреће засебан унутрашњи обилазак. Унутрашњи
обилазак рекурзивно пролази кроз децу `for_statement` чвора и
пријављује сваки `defer_statement` на који наиђе. Кад год је
`defer_statement` пронађен директно у телу петље или у угнежденом
блоку унутар те исте петље, генерише се дијагностика. Наиђе ли се
на угнежден `func` литерал, обилазак у том подстаблу се не зауставља
јер `defer` унутар угнежденог функцијског литерала припада опсегу
те функције, а не спољашње петље.

#figure(
```rust
fn check_for_loop(node: Node, file: &str,
                  diagnostics: &mut Vec<Diagnostic>) {
    for i in 0..node.child_count() {
        if let Some(child) = node.child(i as u32) {
            if child.kind() == "defer_statement" {
                diagnostics.push(/* дијагностика */);
            }
            Self::check_for_loop(child, file, diagnostics);
        }
    }
}
```,
  caption: [Рекурзивна провера `defer` унутар `for_statement` чвора],
) <lst:defer-rs>

== Правила инспирисана књигом _100 Go Mistakes and How to Avoid Them_

=== Генеричко ime пакета (`utility_packages`)

Пакети са именима попут `utils`, `common` или `helpers` окупљају
несродне функционалности без јасне сврхе, чиме отежавају одржавање
и разумевање кода @100mistakes.

#figure(
```go
package utils  // превише генеричко ime пакета
```,
  caption: [Пример генеричког имена пакета],
) <lst:utils-go>

Правило проналази чвор `package_clause` у корену стабла, из њега
чита `package_identifier` и проверава га у унапред одређеном скупу
забрањених имена.

#figure(
```rust
fn is_utility_name(name: &str) -> bool {
    let generic = ["utils", "util", "common",
                   "shared", "base", "helpers", "helper"];
    generic.contains(&name.to_lowercase().as_str())
}
```,
  caption: [Листа забрањених имена пакета],
) <lst:utils-rs>

=== Употреба типа `any` (`no_any`)

Тип `any` (псеудоним за `interface{}`) онемогућава статичку проверу
типова и уноси у код несигурност карактеристичну за динамички типизоване
језике @100mistakes.

#figure(
```go
func process(v any) { ... }          // any у параметру
var cache map[string]interface{} ... // празан интерфејс
```,
  caption: [Употреба `any` и `interface{}`],
) <lst:no-any-go>

Правило обилази стабло и пријављује два случаја. Прво, сваки чвор
`type_identifier` чија је текстуална вредност `any`. Друго, сваки
чвор `interface_type` код кога ниједан именовани потомак није
`method_spec` или `type_identifier`, тј. интерфејс са празним телом.

#figure(
```rust
"type_identifier" => {
    if text == "any" { diagnostics.push(/* ... */); }
}
"interface_type" => {
    let empty = node.named_children(&mut cursor)
        .all(|c| c.kind() != "method_spec"
              && c.kind() != "type_identifier");
    if empty { diagnostics.push(/* ... */); }
}
```,
  caption: [Детекција `any` и `interface{}`],
) <lst:no-any-rs>

=== Позив методе на генеричком параметру (`generics_usage`)

Када генеричка функција позива методу на параметру типа, то је знак
да би конкретан тип директно испунио сврху без генерика @100mistakes.

#figure(
```go
func Print[T Stringer](v T) {
    fmt.Println(v.String()) // позив методе на генеричком T
}
```,
  caption: [Позив методе на генеричком параметру типа],
) <lst:generics-go>

Имплементација је вишефазна. У првој фази из `type_parameter_list`
чвора прикупљају се имена параметара типа. Ако се неки параметар
јавља у повратном типу функције, искључује се из листе (та употреба
генерика је оправдана). У другој фази, имена параметара из листе
функцијских параметара повезују се са именима параметара типа. У
трећој фази, тело функције се претражује за `selector_expression`
чворове чији је `operand` идентификатор из те листе.

#figure(
```rust
if node.kind() == "selector_expression" {
    if let Some(operand) = node.child_by_field_name("operand") {
        if let Ok(name) = operand.utf8_text(source.as_bytes()) {
            if params_to_type_param.contains_key(name) {
                diagnostics.push(/* ... */);
            }
        }
    }
}
```,
  caption: [Детекција позива методе на генеричком аргументу],
) <lst:generics-rs>

=== Уграђени мутекс у структури (`mutex_embedding`)

У Go-у, структура (енг. _struct_) може да угради (енг. _embed_) други
тип тако што га наведе без имена поља. Уграђивање промовише сва јавна
поља и методе угнежденог типа на ниво спољашње структуре, тј. постају
доступне директно преко вредности.

Мутекс (`sync.Mutex`) је примитив за синхронизацију у вишенитном
програмирању: позив `Lock()` блокира нит до момента кад се ресурс
ослободи позивом `Unlock()`. Ако се мутекс угради у структуру уместо
да се деклариша као именовано поље, методе `Lock` и `Unlock` постају
видљиви јавни API те структуре. Свако ко поседује инстанцу може
директно да је закључа или откључа, заобилазећи намеравани интерфејс,
и тако наруши синхронизацију @100mistakes.

#figure(
```go
// Проблематично: Lock и Unlock су јавне методе Cache-а
type Cache struct {
    sync.Mutex
    data map[string]int
}
cache.Lock()   // спољни позивалац може директно да закључа

// Исправно: мутекс је приватан, не излаже своје методе
type Cache struct {
    mu   sync.Mutex
    data map[string]int
}
```,
  caption: [Уграђени наспрам именованог `sync.Mutex`],
) <lst:mutex-go>

Правило проналази чворове `struct_type` и унутар `field_declaration_list`
тражи поља декларације која немају `field_identifier` потомка. Таква
поља су уграђена (без имена). Ако је тип уграђеног поља `sync.Mutex`
или `sync.RWMutex`, генерише се дијагностика.

#figure(
```rust
// Уграђено поље нема field_identifier потомка
if !has_name {
    if let Some(t) = type_node {
        let text = t.utf8_text(source.as_bytes())?;
        if text == "sync.Mutex" || text == "sync.RWMutex" {
            diagnostics.push(/* ... */);
        }
    }
}
```,
  caption: [Детекција уграђеног мутекса по одсуству имена поља],
) <lst:mutex-rs>

=== Сукоб имена са увезеним пакетом (`package_collision`)

Ако локална променљива носи исто ime као увезени пакет, тај пакет
постаје недоступан у том опсегу @100mistakes.

#figure(
```go
import "fmt"

func f() {
    fmt := "текст" // fmt пакет је сад недоступан
    fmt.Println()  // грешка компајлирања
}
```,
  caption: [Локална променљива прекрива увезени пакет],
) <lst:collision-go>

У првој фази прикупљају се локална имена свих увоза из
`import_spec` чворова. У другој фази, рекурзивно се обилази стабло
и проверавају `short_var_declaration`, `var_spec`, `const_spec` и
`parameter_declaration` чворови. Ако je новодекларисано ime присутно
у скупу увезених пакета, пријављује се колизија.

#figure(
```rust
if kind == "short_var_declaration" {
    // за свaki идентификатор на левој страни
    if imports.contains_key(name) {
        diagnostics.push(/* ... */);
    }
}
```,
  caption: [Провера нових декларација наспрам скупа увезених имена],
) <lst:collision-rs>

=== Целобројно прекорачење (`integer_overflow`)

Додељивање литералне вредности изван опсега декларисаног целобројног
типа резултује прекорачењем @100mistakes.

#figure(
```go
var x int8 = 200 // int8 опсег је [−128, 127]
```,
  caption: [Целобројни литерал изван опсега типа],
) <lst:overflow-go>

Правило обилази чворове `var_spec` и `const_spec` и чита тип из
поља `type`. Ако је тип познати целобројни тип, позива се рекурзивни
евалуатор константних израза. Евалуатор подржава целобројне литерале,
унарне операторе и бинарне аритметичке операторе употребом
аритметике са провером прекорачења (`checked_add`, `checked_mul`...).
Добијена вредност се упоређује са границама типа.

#figure(
```rust
fn get_type_limits(t: &str) -> Option<(i128, i128)> {
    match t {
        "int8"  => Some((-128, 127)),
        "int16" => Some((-32768, 32767)),
        "uint8" => Some((0, 255)),
        // ...
        _ => None,
    }
}
```,
  caption: [Табела граница целобројних типова],
) <lst:overflow-rs>

=== Неефикасна иницијализација исечка (`slice_initialization`)

Позив `make([]T, 0)` без аргумента капацитета ствара исечак нулте
дужине и нулте резервисане меморије. Свако накнадно додавање елемената
изазива реалокацију, при чему Go удвостручује позадинске низ @100mistakes.

#figure(
```go
s := make([]int, 0)      // без капацитета
s = append(s, 1, 2, 3)  // вишеструке реалокације
```,
  caption: [Иницијализација исечка без капацитета],
) <lst:slice-init-go>

Правило проналази чворове `call_expression` са функцијом `make`.
Из листе аргумената чита именоване потомке: ако је први аргумент
`slice_type`, а други је литерал `0` и нема трећег аргумента
(капацитет), генерише се дијагностика.

#figure(
```rust
if func_name == "make" && named_children.len() == 2 {
    let is_slice = named_children[0].kind() == "slice_type";
    let len_zero = named_children[1]
        .utf8_text(source.as_bytes()).ok()
        .as_deref() == Some("0");
    if is_slice && len_zero {
        diagnostics.push(/* ... */);
    }
}
```,
  caption: [Детекција `make([]T, 0)` без капацитета],
) <lst:slice-init-rs>

=== Празан литерал уместо нил исечка (`nil_empty_slice`)

Исечак (енг. _slice_) у Go-у је тројка: показивач на позадински низ,
дужина и капацитет. Нил исечак (`var s []T`) је исечак код кога је
показивач `nil`, а дужина и капацитет су нула. Ненил празан исечак
(`[]T{}`) такође има дужину и капацитет нула, али показивач указује на
стварну (иако празну) меморијску локацију.

У пракси ова разлика ретко утиче на понашање програма: оба облика
прихватају `append`, оба имају `len` нула, оба се могу преиначити у
петљи. Значајна разлика се јавља при серијализацији у JSON формат:
нил исечак се серијализује као `null`, а ненил празан исечак као `[]`.
Ако API позивалац очекује поље и добије `null`, може доћи до грешке на
клијентској страни. Поред тога, `[]T{}` изазива мању алокацију коју
Go-ов прикупљач смећа мора касније да обради. Из тих разлога, у Go
заједници важи конвенција да је нил исечак пожељнији подразумевани
облик кад елементи нису унапред познати @100mistakes.

#figure(
```go
s := []int{}   // ненил, нулта дужина; JSON → []
var s []int    // нил исечак, пожељнији; JSON → null
```,
  caption: [Разлика између `[]T{}` и нил исечка],
) <lst:nil-slice-go>

Правило проналази чворове `composite_literal` код којих је `type`
поље `slice_type`, а `value` поље је `literal_value` чвор са нулом
именованих потомака, тј. празним телом литерала.

#figure(
```rust
if node.kind() == "composite_literal" {
    let is_slice = type_node.kind() == "slice_type";
    let is_empty = value_node.kind() == "literal_value"
        && value_node.named_child_count() == 0;
    if is_slice && is_empty { diagnostics.push(/* ... */); }
}
```,
  caption: [Детекција празног литерала исечка],
) <lst:nil-slice-rs>

=== Провера исечка наспрам `nil` (`slice_empty_check`)

Исечак у Go-у може бити у три стања: нил (нису иницијализован),
ненил и празан (иницијализован, али без елемената) и ненил и непразан
(садржи елементе). Оператор `== nil` разликује само прво стање од
преостала два: враћа `true` искључиво за нил исечак. Ненил празан
исечак (`[]int{}` или `make([]int, 0)`) иницијализован је у меморији и
тест `s == nil` враћа за њега `false`, иако је семантички такође "празан".

Последица је да код попут `if s == nil { ... }` не обрађује сва могућа
"празна" стања исечка. Робуснији образац је `len(s) == 0`, јер функција
`len` враћа нулу и за нил и за ненил празан исечак @100mistakes.

#figure(
```go
var s []int
if s == nil { ... }       // не покрива []int{}
if len(s) == 0 { ... }   // исправно
```,
  caption: [Поређење исечка са `nil` насупрот провере дужине],
) <lst:empty-check-go>

Правило је двофазно. У првој фази прикупљају се имена свих
идентификатора чији тип је `slice_type` (из `var_declaration`) или
чија је десна страна доделе иницијализација исечка. У другој фази
обилазе се чворови `binary_expression` са операторима `==` или `!=`
у чијем је операнду `nil`. Ако је суседни операнд идентификатор из
скупа познатих исечака, пријављује се дијагностика.

#figure(
```rust
let slice_side = match (&left, &right) {
    (Some(l), Some(r)) if r.kind() == "nil" => Some(*l),
    (Some(l), Some(r)) if l.kind() == "nil" => Some(*r),
    _ => None,
};
if let Some(node) = slice_side {
    if slice_vars.contains(name) { diagnostics.push(/* ... */); }
}
```,
  caption: [Детекција поређења познатог исечка са `nil`],
) <lst:empty-check-rs>

=== Копирање у исечак нулте дужине (`slice_copy`)

Уграђена функција `copy(dst, src)` копира `min(len(dst), len(src))`
елемената. Ако одредишни исечак има нулту дужину, ниједан елемент
неће бити копиран иако позив не пријављује грешку @100mistakes.

#figure(
```go
var dst []int
copy(dst, src) // dst је нил, len(dst)==0, ништа се не копира
```,
  caption: [Копирање у исечак нулте дужине],
) <lst:copy-go>

У првој фази прикупљају се имена `var_declaration` исечака без
иницијализатора (нил исечак, нулта дужина). У другој фази обилазе
се позиви функције `copy`. Ако је први аргумент (`dst`) или нил
идентификатор из прве фазе, или позив `make([]T, 0)` без капацитета,
пријављује се дијагностика.

#figure(
```rust
let is_problematic = match dst.kind() {
    "identifier" => nil_vars.contains(name),
    _ => self.is_zero_length_make(dst, source),
};
if is_problematic { diagnostics.push(/* ... */); }
```,
  caption: [Провера одредишног аргумента позива `copy`],
) <lst:copy-rs>

=== Нежељени ефекат додавања у подисечак (`slice_append_side_effect`)

Исечак у Go-у је прозор у позадински низ (енг. _backing array_): чува
показивач на тај низ, дужину видљивог прозора и капацитет (колико
места постоји у позадинском низу од почетка прозора). Оператор
исецања `s[low:high]` ствара нови исечак над истим позадинским низом,
само са другачијим почетком и дужином. Кључно је да оба исечка,
оригинал и подисечак, деле исти позадински низ у меморији.

Функција `append` додаје елемент на крај исечка. Ако постоји слободан
простор у капацитету (тј. `len < cap`), нови елемент се записује
директно у позадински низ, не праве се копије. Управо ту лежи проблем:
ако подисечак `s[1:3]` има капацитет 3 (јер оригинал иза индекса 3
има места), `append` ће нови елемент уписати на позицију `s[3]` у
позадинском низу, тиме мењајући оригинални исечак без икаквог
упозорења. Ово је особито опасно кад се подисечак и оригинал користе
у различитим деловима кода @100mistakes.

Решење је тзв. трећи индекс: `s[low:high:high]` изричито поставља
капацитет подисечка на `high - low`, тј. на тачно његову дужину. При
следећем `append` капацитет је исцрпљен па Go мора да алоцира нови
позадински низ, чиме се прекида дељење меморије.

#figure(
```go
s := []int{1, 2, 3, 4}
sub := s[1:3]
sub = append(sub, 99) // може да промени s[3]
```,
  caption: [Додавање у подисечак са дељеним позадинским низом],
) <lst:append-go>

Правило проналази позиве функције `append` и проверава да ли је
prvi аргумент чвор `slice_expression`. Ако `slice_expression` нема
поље `capacity` (тј. није трећи индекс `s[low:high:max]`),
пријављује се дијагностика. Трећи индекс изричито ограничава
капацитет подисечка и тиме спречава нежељени ефекат.

#figure(
```rust
if func == "append" {
    if let Some(first) = args.named_child(0) {
        if first.kind() == "slice_expression"
            && first.child_by_field_name("capacity").is_none()
        {
            diagnostics.push(/* ... */);
        }
    }
}
```,
  caption: [Детекција додавања у двоиндексни подисечак],
) <lst:append-rs>

=== Измена копије у `range` петљи (`range_copy_modification`)

Вредносна променљива `range` петље (`for i, v := range slice`) је
копија елемента из исечка, а не референца. Додељивање пољу те
копије не мења оригинал @100mistakes.

#figure(
```go
for _, item := range items {
    item.Value = 42 // мења копију, не оригинал
}
```,
  caption: [Погрешна измена поља копије у `range` петљи],
) <lst:range-copy-go>

Правило прегледа `for_statement` чворове са `range_clause`. Из
левог поља клаузуле чита se вредносна променљива (други именовани
идентификатор, ако постоји и није `_`). Затим се тело петље
претражује за `assignment_statement` чворове чији је леви операнд
`selector_expression` чији `operand` одговара вредносној
променљивој. Такви записи мењају копију и пријављују се.

#figure(
```rust
fn check_selector_write(node, value_var, ...) {
    if node.kind() == "selector_expression" {
        let operand = node.child_by_field_name("operand")?;
        if operand.utf8_text(...)? == value_var {
            diagnostics.push(/* ... */);
        }
    }
}
```,
  caption: [Детекција записа у поље вредносне `range` променљиве],
) <lst:range-copy-rs>

=== Хватање адресе `range` променљиве (`range_pointer_capture`)

У верзијама Go-а пре 1.22, петљом `for i, v := range slice` компајлер
декларише промењиве `i` и `v` само једном, пре прве итерације, и
ажурира им вредности у свакој наредној итерацији. То значи да `i` и
`v` у свим итерацијама заузимају исту меморијску локацију. Оператор
адресе `&v` враћа адресу те локације, а не адресу конкретне вредности
коју `v` тренутно носи.

Ако се та адреса чува у неком контејнеру (листи показивача, горутини
и сл.), по завршетку петље сви сачувани показивачи показују на исто
место у меморији — које садржи вредност из последње итерације. Грешка
се веома тешко уочава јер код у моменту чувања изгледа исправан, а
погрешно понашање се манифестује тек при каснијем приступу
сачуваним показивачима. Ово је посебно учестала грешка у конкурентном
коду где горутине хватају `&v` пре него што петља заврши. Верзија
Go 1.22 мења ово понашање тако да свака итерација добија нову
променљиву, чиме грешка нестаје, али код написан за старије верзије
остаје угрожен @100mistakes.

#figure(
```go
var ptrs []*int
for _, v := range nums {
    ptrs = append(ptrs, &v) // сви елементи показују на исти v
}
```,
  caption: [Хватање адресе `range` променљиве],
) <lst:range-ptr-go>

Правило прегледа `for_statement` чворове са `range_clause` и
прикупља имена свих невредносних идентификатора на левој страни.
Затим претражује тело петље за чворове `unary_expression` са
оператором `&`, чији је операнд идентификатор из тог скупа.

#figure(
```rust
if node.kind() == "unary_expression" {
    let op = node.child(0)?.utf8_text(...)? ;
    if op == "&" {
        let operand = node.child(1)?;
        if range_vars.iter().any(|v| v == name) {
            diagnostics.push(/* ... */);
        }
    }
}
```,
  caption: [Детекција `&` оператора над `range` променљивом],
) <lst:range-ptr-rs>

=== Бројање бајтова уместо rune-ова (`string_rune`)

У Go-у, стринг је низ бајтова, а не низ знакова. Кодовање UTF-8,
које Go подразумевано користи, представља ASCII знакове једним бајтом,
али знакови других писама (ћирилица, кинески, јапански) или емоџи
захтевају два до четири бајта. Због тога `len(s)` враћа број бајтова,
а не број видљивих знакова: за стринг `"Здраво"` резултат је 12 (6
знакова по 2 бајта), а не 6.

Термин rune у Go-у означава Unicode кодну тачку (тип `int32`), тј.
један видљиви знак без обзира на то колико бајтова заузима. Замена
за `len` при бројању знакова је `utf8.RuneCountInString(s)`.

Слична конфузија настаје при итерацији стринга. Облик
`for i := range s` иде кроз rune позиције (у бајтовима) и `i` добија
вредности 0, 2, 4, ... за ћирилични стринг. Ако се унутар петље
приступи стрингу индексирањем `s[i]`, резултат је бајт (`uint8`), не
rune. Мултибајтни знак тако бива исечен на први бајт, и добија се
погрешна вредност. Исправан облик је `for i, r := range s` где `r`
носи цео rune @100mistakes.

Правило детектује конверзију `string(небајт-израз)` јер такав стринг
скоро сигурно садржи мултибајтне знакове, па `len` над њим брои
бајтове уместо знакова.

#figure(
```go
s := string(someBytes)
fmt.Println(len(s))  // број бајтова, не број знакова

for i := range s {
    fmt.Println(s[i]) // бајт, не rune
}
// Исправно:
fmt.Println(utf8.RuneCountInString(s))
for _, r := range s { fmt.Println(r) }
```,
  caption: [Погрешна употреба `len` и индексирање, и исправни облици],
) <lst:rune-go>

Правило је двофазно са два независна обрасца. У фази прикупљања
подаци се деле у два скупа: `string_vars` (идентификатори стринг
типа или стринг литерала) и `byte_string_vars` (идентификатори
добијени конверзијом `string(небајт-литерал)`). Први образац пријављује
позив `len` чији је аргумент члан `byte_string_vars`. Други образац
проналази `for_statement` са `range_clause` над чланом `string_vars`
где лева страна има тачно један невредносни идентификатор, па у
телу петље тражи `index_expression` облика `s[i]`.

#figure(
```rust
// Образац 1: len на бајт стрингу
if func == "len" && byte_string_vars.contains(arg_name) {
    diagnostics.push(/* ... */);
}
// Образац 2: s[i] у индексном range обиласку
if operand_ok && index_ok {
    diagnostics.push(/* ... */);
}
```,
  caption: [Два обрасца детекције грешке rune/бајт конфузије],
) <lst:rune-rs>
