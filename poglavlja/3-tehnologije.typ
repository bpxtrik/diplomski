#import "../funkcije.typ": todo

= Технологије
<tehnologije>

У овом поглављу описане су кључне технологије коришћене при изради алата:
програмски језик Rust, библиотека за парсирање Tree-sitter, концепти
конкретног и апстрактног стабла синтаксе, и библиотека `clap` за обраду
аргумената командне линије.

== Програмски језик Rust

Rust је системски програмски језик опште намене
@matsakis2014rust @rust. Главна особина по којој се Rust издваја међу
системским језицима јесте гаранција безбедности меморије без употребе
сакупљача смећа (енг. _garbage collector_).

Безбедност меморије постиже се кроз систем власништва (енг. _ownership_)
и позајмљивања (енг. _borrowing_). Компајлер прати власника сваке вредности
у меморији и аутоматски ослобађа меморију када власник изађе из опсега.
Систем позајмљивања прецизно одређује колико референци може постојати у
исто време. Дозвољен је произвољан број непроменљивих (енг. _immutable_)
референци или тачно једна променљива (енг. _mutable_) референца, никада
обоје истовремено. Ова правила компајлер проверава у потпуности при
превођењу, а не у току извршавања @rustbook. Резултат је елиминација
читаве класе грешака (употреба ослобођене меморије, енг. _use-after-free_;
трка података, енг. _data race_; нулти показивачи) без додатних трошкова
у току извршавања.

Rust је изабран за имплементацију алата из неколико разлога. Прво,
алат потенцијално обрађује велики број фајлова, па је перформанса
важна. Будући да се Rust компајлира директно у машински код, не постоји
трошак покретања виртуелне машине нити паузе сакупљача смећа. Друго,
систем власништва онемогућава читаву класу имплементационих грешака
унутар самог алата, чиме је обезбеђена поузданост. Треће, Rust нуди
богат екосистем библиотека преко регистра `crates.io`, међу којима
су Tree-sitter и `clap` библиотеке употребљене у овом
пројекту.

== Tree-sitter

Tree-sitter је библиотека за инкрементално парсирање изворног кода
@treesitter. Данас се
употребљава у широком спектру програмских едитора и алата, укључујући
Neovim, Emacs и GitHub интерфејс, за истицање синтаксе, навигацију
кодом и структурно претраживање.

Систем се састоји из два дела: генератора парсера и извршне библиотеке
(енг. _runtime_). Генератор прима формалну граматику одређеног
програмског језика и из ње производи парсер у програмском језику C.
Извршна библиотека учитава тај парсер у току рада програма и употребљава
га за парсирање изворног текста. Захваљујући овој архитектури,
Tree-sitter је у потпуности независан од конкретног програмског
језика; за сваки подржани језик постоји засебан пакет са граматиком.

У пројекту су коришћена два Rust пакета: `tree-sitter`, који садржи
извршну библиотеку са API-јем за Rust, и `tree-sitter-go`, који садржи
граматику за програмски језик Go.

Важна особина Tree-sitter-а је подршка за инкрементално парсирање: при
малој промени изворног кода библиотека репарсира само измењене делове
стабла уместо целог документа. Иако ова особина није искоришћена у алату
за статичку анализу, пошто се фајлови читају само по један пут, она је
главни разлог широке употребе Tree-sitter-а у едиторима кода где је
брзина одговора кључна.

== Конкретно наспрам апстрактног стабла синтаксе

Оба облика стабла синтаксе представљају синтаксну структуру програма
у виду стабла, али садрже различит ниво детаља.

*Апстрактно стабло синтаксе* (АСС, енг. _Abstract Syntax Tree_) уклања
из стабла синтаксну „буку", тј. кључне речи, интерпункцију и размаке, и
чува само семантички значајне елементе. На пример, у изразу `a + b` АСС
садржи чвор за бинарну операцију са два потомка (`a` и `b`), а оператор
`+` је имплицитан у типу чвора, а не засебан чвор. Алати попут `go vet` и
`staticcheck` раде над АСС-ом добијеним директно од Go компајлера, уз
информације о типовима, чиме добијају пуну семантичку слику програма.
Последица је, чврста зависност од Go инфраструктуре: `go vet`
је уграђен у Go ланац алата, а `staticcheck` захтева да пројекат буде
компајлабилан.

*Конкретно стабло синтаксе* (КСС, енг. _Concrete Syntax Tree_)
производи Tree-sitter, чува сваки токен изворног кода: заграде, зарезе,
кључне речи и тачке-зарезе. Ово стабло представља целу репрезентацију
изворног кода. Из КСС-а је могуће реконструисати оригинални текст у
потпуности. Сваки чвор у стаблу носи прецизну позицију у изворном коду
(ред и колону), па је могуће детектовати проблем и пријавити га кориснику
са тачном локацијом.

Листинг @lst:kss-primer илуструје КСС структуру коју Tree-sitter производи
за функцију `func add(a, b int) int { return a + b }`.

#figure(
```
(source_file
  (function_declaration
    name: (identifier)            // "add"
    parameters: (parameter_list
      "("
      (parameter_declaration
        name: (identifier)        // "a"
        name: (identifier)        // "b"
        type: (type_identifier))  // "int"
      ")")
    result: (type_identifier)     // "int"
    body: (block
      "{"
      (return_statement
        "return"
        (binary_expression
          left:  (identifier)     // "a"
          right: (identifier)))   // "b"
      "}")))
```,
  caption: [КСС структура Tree-sitter парсера за кратку Go функцију],
) <lst:kss-primer>

Tree-sitter разликује именоване (енг. _named_) и анонимне (енг. _anonymous_)
чворове. Именовани чворови, попут `function_declaration`,
`parameter_list` или `binary_expression`, одговарају граматичким
нетерминалима и носе семантичку тежину. Анонимни чворови представљају
литералне токене из граматике, попут `"("`, `"{"` или `"return"`, и
приказани су под наводницима у листингу @lst:kss-primer. Употребом
методе `named_children` API-ја могу се добити само именовани потомци
чвора, чиме се поједностављује обилазак стабла у правилима где
анонимни чворови нису потребни.

Употреба КСС-а носи са собом значајну предност: алат не захтева ниједну
компоненту Go инфраструктуре нити информације о типовима. Анализа се
обавља искључиво над синтаксном структуром фајла, без претходног
компајлирања пројекта.

== Библиотека `clap`

`clap` (енг. _Command Line Argument Parser_) је Rust библиотека за
декларативну дефиницију аргумената командне линије @clap. Уместо ручног
парсирања низа аргумената, програмер дефинише структуру са атрибутима,
а `clap` аутоматски генерише парсер, поруке о грешкама и помоћни текст
при позиву са заставицом `--help`.

#figure(
```rust
#[derive(Parser, Debug)]
#[command(name = "golint")]
#[command(about = "Lints Go source files")]
pub struct Args {
    #[arg(value_name = "PATH")]
    pub path: PathBuf,

    #[arg(short, long, value_name = "FILE")]
    pub output: Option<PathBuf>,
}
```,
  caption: [Дефиниција аргумената употребом `clap` библиотеке],
) <lst:clap-struct>

Атрибут `#[derive(Parser)]` инструира `clap` да аутоматски генерише
имплементацију парсера за дату структуру. Поље `path` је обавезан
позициони аргумент који прима путању до Go фајла или директоријума.
Атрибут `#[arg(short, long)]` на пољу `output` означава да је реч о
опционом именованом аргументу доступном у облику `-o FILE` или
`--output FILE`. Ако аргумент није наведен, вредност је `None` и
дијагностике се исписују на стандардни излаз.
